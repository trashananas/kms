# Полочка 📦 — бесплатный обменник вещей

**Полочка** — современная платформа бесплатного обмена вещами для родителей Клуба многодетных семей Выхино-Жулебино. Отдавай ненужное, забирай полезное — всё внутри района.

## 🚀 Возможности

- **Регистрация и вход** по номеру телефона + пароль. Поддержка «Запомнить меня». Телефоны нормализуются (8..., +7..., 10-значные — все принимаются).
- **Объявления** с фото/видео (до 5 файлов), возрастными метками, ценой, адресом.
- **Категории и подкатегории** — встроенные и пользовательские. Любой пользователь может создать новую категорию прямо при добавлении объявления.
- **Фильтр по категории** — выберите категорию в виде чипа над списком, и лента мгновенно отфильтруется.
- **Фильтр по возрасту** — множественный выбор возрастных меток.
- **Бронирование** — забронируйте понравившуюся вещь, продавец и покупатель видят статус брони.
- **Режим Тикток 📱** *(только мобильные)* — вертикальная лента объявлений с плавной прокруткой и кнопкой ❤️.
- **Режим Тиндер 💘** *(только мобильные)* — карточки с свайпом: вправо = лайк, влево = скрыть.
- **Профиль** с вкладками: данные, мои объявления, лайки, скрытые, брони.
- **UI-уведомления** — ошибки и успешные действия отображаются тостами и встроенными сообщениями, без системных `alert()`.

## 🛠 Запуск локально

```bash
npm install
cp .env.example .env
# (опционально) добавьте YANDEX_MAPS_API_KEY в .env
npm start
# открыть http://localhost:3000
```

## ⚙️ Конфигурация

Поместите API‑ключ Яндекс.Геокодер в файл `.env`:

```text
YANDEX_MAPS_API_KEY=ваш-ключ
```

Ключ используется только на сервере как прокси — клиенту не передаётся.

## 🚢 Деплой на Vercel

1. Зайдите в [dashboard.vercel.com](https://vercel.com/dashboard) → ваш проект → **Settings → Environment Variables**.
2. Добавьте `YANDEX_MAPS_API_KEY` со значением ключа от **«JavaScript API и HTTP Геокодер»**.
3. `vercel.json` в корне уже настроен для Express-приложения.

> На Vercel файловая система read-only, поэтому данные (`db.json`) хранятся в `/tmp`. При перезапуске функции данные могут сброситься — для продакшена рекомендуется подключить внешнюю БД.

## 📦 Стек

- **Бэкенд**: Node.js + Express, данные в `db.json`
- **Фронтенд**: Vanilla JS, CSS с эффектами glassmorphism, без фреймворков
- **Деплой**: Vercel (serverless) или любой Node-хостинг

## 🔮 Дальнейшее развитие

- Заменить JSON-файл на настоящую БД (SQLite / Postgres / MongoDB)
- Хеширование паролей (bcrypt) и JWT-сессии
- Административная панель и роли модераторов
- Push-уведомления о новых объявлениях в выбранных категориях
